package pl.peepolab.integration.slack.infrastructure

import jakarta.inject.Singleton

@Singleton
class SlackUserDao(
) {

//    fun findSlackUser(slackUserId: SlackUserId): SlackUser? {
//
//        return context.selectFrom(SLACK_USER)
//            .where(SLACK_USER.SLACK_USER_ID.eq(slackUserId))
//            .fetchOneInto(SlackUser::class.java)
//    }

}